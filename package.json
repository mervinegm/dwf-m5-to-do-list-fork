{
  "name": "to-do-marce",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.0-rc.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /dwf-m5-parcel-todo",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
